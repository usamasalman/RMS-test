const http = require('http'); const req = http.request('http://localhost:3001/api/v1/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' } }, (res) = let data = ''; res.on('data', chunk = += chunk); res.on('end', () = const body = JSON.parse(data); const token = body.token; const req2 = http.request('http://localhost:3001/api/v1/risks/2', { method: 'PUT', headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + token } }, (res2) = let data2 = ''; res2.on('data', chunk = += chunk); res2.on('end', () = Status:', res2.statusCode, data2)); }); req2.write(JSON.stringify({ title: 'Updated Title', description: 'Updated Desc', likelihood: 4, impact: 4, status: 'Open' })); req2.end(); }); }); req.write(JSON.stringify({ email: 'admin@grc.com', password: 'password' })); req.end(); 
